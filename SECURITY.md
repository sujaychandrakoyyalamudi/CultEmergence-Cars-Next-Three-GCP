# Security policy

Report vulnerabilities privately to the repository owner. Do not include credentials, production data, or exploit traffic against the public domain.

This application is read-only, accepts no accounts or payments, and sends no analytics by default. Security headers are defined in `next.config.ts`; infrastructure edge controls are in `infra/terraform`.
